{
  "name": "mancera-personal-data-pipeline",
  "version": "1.0.0",
  "private": true,
  "description": "Portfolio-ready full-stack GitHub analytics pipeline with OAuth, Supabase, cron sync, n8n webhooks, and an AI chatbot.",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:web\"",
    "dev:server": "npm --prefix server run dev",
    "dev:web": "npm --prefix web run dev",
    "install:all": "npm --prefix server install && npm --prefix web install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
